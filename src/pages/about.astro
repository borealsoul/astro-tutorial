---
import BaseLayout from "../layouts/BaseLayout.astro";
const tituloPagina = "Sobre Mim";

const identidade = {
    primeiroNome: "Aurora",
    pais: "Brasil",
    ocupacao: "Estudante de TI",
    hobbies: [
        "fotografia",
        "edição de vídeo",
        "namorar a melhor pessoa do mundo :3",
    ],
};

const habilidades = ["HTML", "CSS", "Gleam", "Rust", "Regex"];
---

<BaseLayout tituloPagina={tituloPagina}>
    <h2>{tituloPagina}...</h2>
    <h2>... e meu site Astro!</h2>

    <p>
        Espero que tudo funcione, depois do Netlify ter banido minha conta de
        graça??
    </p>
    <p>Obrigada Cloudflare Pages :3</p>

    <h3>Alguns fatos sobre mim...</h3>
    <p>Meu nome é {identidade.primeiroNome}</p>
    <p>Moro no {identidade.pais}, e sou uma {identidade.ocupacao}</p>
    {
        identidade.hobbies.length >= 2 && (
            <p>
                Dois dos meus hobbies são {identidade.hobbies[0]} e{" "}
                {identidade.hobbies[1]}
            </p>
        )
    }
    <h3>Minhas habilidades são:</h3>
    <ul>
        {habilidades.map((habilidade) => <li>{habilidade}</li>)}
    </ul>
</BaseLayout>
