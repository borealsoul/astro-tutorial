---
import BaseLayout from "./BaseLayout.astro";
const { frontmatter } = Astro.props;
---

<BaseLayout tituloPagina={frontmatter.title}>
    <p>Escrito por {frontmatter.author}</p>
    <p>Publicado em {frontmatter.pubDate}</p>
    <slot />
</BaseLayout>
